<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ibm.gbs.gbs_cai_web.mapper.TestMapper">
    <cache />
    <select id="getObjectById" resultType="hashmap" parameterType="String">
        SELECT *	   
        FROM member
        WHERE id=#{id}
    </select>
</mapper>